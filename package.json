{
  "name": "echos-vault",
  "version": "1.0.0",
  "description": "Echo's Vault — Collectibles Command Center",
  "scripts": {
    "start": "npx http-server . -p ${PORT:-8080} -c86400 --cors --gzip -d false",
    "dev": "npx http-server . -p 8765 -c-1 --cors"
  },
  "dependencies": {
    "http-server": "^14.1.1"
  }
}
